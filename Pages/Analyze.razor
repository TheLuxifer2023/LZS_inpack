@page "/analyze"
@using LZS_Web.Services
@inject PhyreEngineService PhyreService
@inject FileProcessingService FileService

<PageTitle>Analyze - LZS Phyre Engine Tool</PageTitle>

<div class="container-fluid">
    <h1>🔍 Analyze Phyre Files</h1>
    <p class="lead">Детальный анализ структуры .phyre файлов</p>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5>1. Загрузите .phyre файл для анализа</h5>
                </div>
                <div class="card-body">
                    <InputFile OnChange="@OnFileSelected" accept=".phyre" class="form-control" maxAllowedSize="100_000_000" />
                    @if (!string.IsNullOrEmpty(fileName))
                    {
                        <div class="mt-3">
                            <div class="alert alert-success">
                                <strong>Файл загружен:</strong> @fileName<br/>
                                <strong>Размер:</strong> @FileService.GetFileSize(fileSize)
                            </div>
                        </div>
                    }
                </div>
            </div>

            @if (!string.IsNullOrEmpty(filePath))
            {
                <div class="card mt-3">
                    <div class="card-header">
                        <h5>2. Выберите тип анализа</h5>
                    </div>
                    <div class="card-body">
                        <div class="form-group mb-3">
                            <label class="form-label">Анализ:</label>
                            <select @bind="analysisType" class="form-select">
                                <option value="analyze">Общий анализ структуры</option>
                                <option value="finddata">Найти данные шрифта</option>
                                <option value="findsize">Определить размер структуры</option>
                            </select>
                        </div>

                        @if (analysisType == "findsize")
                        {
                            <div class="form-group mb-3">
                                <label class="form-label">Offset:</label>
                                <input type="number" @bind="structOffset" class="form-control" placeholder="3446" />
                            </div>
                        }

                        <button class="btn btn-primary" @onclick="StartAnalysis" disabled="@isProcessing">
                            @if (isProcessing)
                            {
                                <span class="spinner-border spinner-border-sm me-2"></span>
                            }
                            Начать анализ
                        </button>
                    </div>
                </div>
            }
        </div>

        <div class="col-md-6">
            @if (isProcessing)
            {
                <div class="card">
                    <div class="card-header">
                        <h5>🔄 Анализ...</h5>
                    </div>
                    <div class="card-body">
                        <div class="progress">
                            <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" style="width: 100%"></div>
                        </div>
                        <p class="mt-2">Выполняется анализ файла...</p>
                    </div>
                </div>
            }

            @if (!string.IsNullOrEmpty(resultOutput))
            {
                <div class="card">
                    <div class="card-header">
                        <h5>📋 Результат анализа</h5>
                    </div>
                    <div class="card-body">
                        <pre class="bg-light p-3 rounded">@resultOutput</pre>
                        
                        @if (hasError)
                        {
                            <div class="alert alert-danger mt-3">
                                <strong>Ошибка:</strong> @resultError
                            </div>
                        }
                        else
                        {
                            <div class="alert alert-success mt-3">
                                <strong>Анализ завершен!</strong>
                            </div>
                        }
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5>💡 Типы анализа</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <h6>🔍 Общий анализ</h6>
                            <ul>
                                <li>Magic bytes и версия</li>
                                <li>Количество классов</li>
                                <li>Список классов</li>
                                <li>Количество экземпляров</li>
                                <li>Offsets и размеры</li>
                            </ul>
                        </div>
                        <div class="col-md-4">
                            <h6>📝 Найти данные шрифта</h6>
                            <ul>
                                <li>Поиск PBitmapFontCharInfo</li>
                                <li>Абсолютный offset</li>
                                <li>Количество символов</li>
                                <li>Размер структуры</li>
                                <li>Готовые параметры для извлечения</li>
                            </ul>
                        </div>
                        <div class="col-md-4">
                            <h6>📏 Размер структуры</h6>
                            <ul>
                                <li>Анализ последовательных кодов</li>
                                <li>Определение размера записи</li>
                                <li>Первый и следующий символ</li>
                                <li>Валидация структуры</li>
                                <li>Рекомендуемый размер</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private string filePath = string.Empty;
    private string fileName = string.Empty;
    private long fileSize = 0;
    private string analysisType = "analyze";
    private int structOffset = 3446;
    private bool isProcessing = false;
    private string resultOutput = string.Empty;
    private string resultError = string.Empty;
    private bool hasError = false;

    private async Task OnFileSelected(InputFileChangeEventArgs e)
    {
        if (e.File != null)
        {
            try
            {
                filePath = await FileService.SaveBrowserFileAsync(e.File);
                fileName = e.File.Name;
                fileSize = e.File.Size;
                ClearResults();
            }
            catch (Exception ex)
            {
                resultError = $"Ошибка загрузки файла: {ex.Message}";
                hasError = true;
            }
        }
    }

    private async Task StartAnalysis()
    {
        if (string.IsNullOrEmpty(filePath))
            return;

        isProcessing = true;
        ClearResults();

        try
        {
            ProcessResult result;

            switch (analysisType)
            {
                case "analyze":
                    result = await PhyreService.AnalyzeFileAsync(filePath);
                    break;
                case "finddata":
                    result = await PhyreService.FindFontDataAsync(filePath);
                    break;
                case "findsize":
                    result = await PhyreService.FindStructSizeAsync(filePath, structOffset);
                    break;
                default:
                    throw new ArgumentException("Неизвестный тип анализа");
            }

            resultOutput = result.Output;
            resultError = result.Error;
            hasError = !result.Success;
        }
        catch (Exception ex)
        {
            resultError = $"Ошибка выполнения анализа: {ex.Message}";
            hasError = true;
        }
        finally
        {
            isProcessing = false;
        }
    }

    private void ClearResults()
    {
        resultOutput = string.Empty;
        resultError = string.Empty;
        hasError = false;
    }
}
